import React from 'react';

interface NavbarProps {

}

const Navbar: React.FC<NavbarProps> = ({}) => {
    return (
        <header>
            this is the nav bar
        </header>
    );
}
export default Navbar;